import styled from 'styled-components'

export const Link = styled.a`
  padding: 1.5rem 0 0 1.5rem;
  font-size: 1.25rem;
  display: flex;
  align-items: center;
  color: #4d4c52;

  & svg {
    margin-right: 0.6rem;
  }
`
